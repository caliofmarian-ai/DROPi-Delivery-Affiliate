{
  "name": "dropi-delivery-affiliate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check src/content.mjs && node --check src/site.mjs"
  }
}
